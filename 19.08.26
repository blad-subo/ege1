#########################################19.08.26####################################################
# У почтового фильтра есть множество слов-триггеров.
# Разбейте письмо на слова и проверьте пересечением:
# если общие слова есть —
# письмо в спам, покажите, что именно сработало.

# later = set(input())
#
# splited_msg = set(later.split()) & filter
#
# filter = {"gay", "her", "negr"}
# if splited_msg:
#     print("письмо в спаме", splited_msg)



# Занятые слоты расписания — множество кортежей
# (день, час): элементом множества может быть кортеж,
# и это открывает дорогу к «составным» ключам.
# Проверьте список желаемых слотов и выведите свободные.

# busy = {("пн", 10), ("пн", 12), ("вт", 10), ("ср", 16)}
# want = [("пн", 10), ("ср", 14), ("вт", 10), ("пт", 12)]
#
# s = set(want) - busy
# print(s)

########################################словарь###############################################
# s = {"key":"walue"} #mapping
# s["key"] = 10
# print(s)

# for i in s:
#     print(i)

# s = {"key1":"value1",
#      "key2":"value2",
#      "key3":"value3"}



#print(s.values()) #список значений
# print(list(s.keys())) #список ключей
# print(list(s.items())) #([('key1', 'value1'), ('key2', 'value2'), ('key3', 'value3')])

#pop - удаляет элемент по ключу
# s.pop("key1")
# print(s1, s)

# print(s.popitem()) #удаляет и возвращает последний элемент
# print(s)

# s["key4"] = 10 #добавили (не было ключа)
# s["key3"] = 39 #изменили
# del s["key1"]

#если ключ есть то значение если нет ключа то default
# print(s.get("key4", 10))

# s.update({'key5':10, "key1":10000})
# print(s)

# s1 = s.copy()
# print(id(s1), id(s))

# print("key1" in s)

# s = [("key1","value1"),("key2","value2"),("key3","value3")]
# print(dict(s))
# print()

#пользователь вводит пары a:1 b:1 c:1

# s = input()
# d = []
# s1 = s.split()
# for i in s1:
#     key, value = i.split(":")
#     # d[key] = value
#     d.append((key, value))
#
# print(dict(d))

# Дан список имен учеников и их оценок:
# students = ["Анна", 5, "Борис", 4, "Вера", 5]
# Создайте словарь из этого списка, где ключами будут имена, а значениями — оценки.

# grades = {}
# students = ["Анна", 5, "Борис", 4, "Вера", 5]
# for i in range(0,len(students),2):
#     print(students[i], students[i+1])
#     # grades[students[i]] = students[i+1]
#     grades.update({students[i]:students[i+1]})
# print(grades)
