#номер 1
# version = ["1.4.12", "1.10.2", "1.4.9", "1.2.30",]
# tuple(version)
# for i in range(len(version)-1):
#     if version[i] < version[i+1]:
#         print(version[i+1])

# Робот-пылесос стартует в точке (0, 0) и смотрит на север. Прошивка исполняет команды-кортежи:
# 1. ("шаг", n) — проехать n клеток вперёд,
# 2. ("поворот", "направо") или ("поворот", "налево") — повернуться на месте.
# Определите, где робот закончит уборку, выполняя следующие команды:
# ```json
# [("шаг", 3), ("поворот", "направо"), ("шаг", 2), ("поворот", "направо"), ("шаг", 1),
#  ("поворот", "налево"), ("шаг", 4)]
#
# ```
# Ключевая идея: направление тоже храним кортежом-вектором — север это (0, 1),
# восток (1, 0), — тогда шаг превращается в прибавление вектора,
# а поворот направо — в перестановку (dy, -dx).

# json = [("шаг", 3), ("поворот", "направо"), ("шаг", 2), ("поворот", "направо"), ("шаг", 1),
# ("поворот", "налево"), ("шаг", 4)]
# wos = (1, 0)
# sev = (0, 1)
# start = (0,0)
# now = (0,0)
# for i in range(len(json)):
#     if json[i] == ("шаг", 3):
#         lst = list(now)
#         lst[0] += 3
#         now = tuple(lst)
#         print(now)
#     if json[i] == ("поворот", "направо"):
#         pst = list(now)
#         pst[0] = 0
#         now = pst[1] = -now[0]
#         print(now)


# На складе интернет-магазина каждая запись учёта — кортеж (ряд, полка, товар).
# Роботу-кладовщику нужно уметь три вещи: найти все ячейки с нужным товаром, проверить,
# свободна ли конкретная ячейка под новую поставку, и распечатать опись ряда по порядку полок.
# На складе лежат следующие товары:
#     (1, 1, "клавиатуры"),
#     (1, 2, "мыши"),
#     (2, 1, "мониторы"),
#     (2, 3, "веб-камеры"),
#     (3, 2, "мыши"),
# Робот должен определить:
# 1. Где лежат мыши
# 2. Свободна ли ячейка (2, 2)
# 3. Какие предметы лежат на первом ряду


# pl = ((1, 1, "клавиатуры"),(1, 2, "мыши"),(2, 1, "мониторы"),(2, 3, "веб-камеры"),(3, 2, "мыши"))
# for i in range(len(pl)):
#     if pl[i][2] == "мыши":
#         print("мыши тут:", pl[i][0:2:])
# for j in range(len(pl)):
#     if pl[j][0] == 1:
#         print("на первом ряду:", pl[j][2])
# for m in range(len(pl)):
#     if (2, 2) not in pl[m]:
#         print("ячейка (2, 2) свободна")
#         break
